{
  "name": "fitin",
  "version": "1.0.0",
  "private": true,
  "description": "FitIn — Free fitness & meal planning app",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace=apps/web",
    "build:web": "npm run build --workspace=apps/web",
    "dev:mobile": "npm run start --workspace=apps/mobile"
  }
}
